# Phase 1 Evidence Report

## 1. Phase 1 Status

COMPLETE. Hero copy reconciled to the approved source of truth. Regression protection added. No assets, routes, or unrelated sections modified. Nothing committed, pushed, published, or deployed.

## 2. Hero Comparison Result

MATCH after reconciliation. Live `getCopy("v3").hero` fields equal the approved strings exactly.

## 3. Files Inspected

- `src/components/cyryx/Hero.tsx`
- `src/copy/v3.ts`
- `src/copy/index.ts`
- `src/copy/types.ts`
- `src/copy/schema.ts`
- `tests/accessibility/copy-validation.spec.ts`
- `tests/accessibility/hero.spec.ts`
- `tests/accessibility/hero-video.spec.ts`
- `tests/accessibility/hero-video-lazy.spec.ts`
- `src/assets/cyryx-hero-monolith-v2-{640,1280,1920}.{webp,avif}.asset.json`
- `src/assets/cyryx-hero.mp4.asset.json`
- `.lovable/plan.md`

## 4. Files Modified

- `src/copy/v3.ts` — 4 hero string fields only.
- `tests/accessibility/copy-validation.spec.ts` — added `Hero — approved copy lock` describe block.
- `.lovable/plan.md` — plan document (non-shipping).
- `src/routeTree.gen.ts` — auto-regenerated by the TanStack Router plugin (not hand-edited).

No other source files modified.

## 5. Exact Copy Before and After

`src/copy/v3.ts` → `hero`:

| Field | Before | After (= Approved) |
|---|---|---|
| headline | `The execution layer for operational AI.` | `The execution layer for enterprise AI.` |
| sub | `AI products and execution systems — governed agents, automated workflows, and operational infrastructure engineered for accountability and cost control.` | `Cyryx Labs builds AI products and execution systems — governed agents, automated workflows, and operational infrastructure engineered for accountability, auditability, and cost control.` |
| ctaPrimary (→ `#contact`) | `Explore MAAX Studio` | `Start a project` |
| ctaSecondary (→ `#maax`) | `Start a project` | `MAAX Studio →` |

`meta` and `rail` remained `[]`. No other keys touched.

## 6. Asset References Before and After

Identical before and after. `Hero.tsx` continues to reference:

- `src/assets/cyryx-hero-monolith-v2-640.webp.asset.json`
- `src/assets/cyryx-hero-monolith-v2-1280.webp.asset.json`
- `src/assets/cyryx-hero-monolith-v2-1920.webp.asset.json`
- `src/assets/cyryx-hero-monolith-v2-640.avif.asset.json`
- `src/assets/cyryx-hero-monolith-v2-1280.avif.asset.json`
- `src/assets/cyryx-hero-monolith-v2-1920.avif.asset.json`
- `src/assets/cyryx-hero.mp4.asset.json`

No image, video, poster, `srcSet`, `sizes`, or fetch strategy changed.

## 7. Regression Protection Added or Confirmed

Added in `tests/accessibility/copy-validation.spec.ts`, describe block `Hero — approved copy lock`:

1. `v3 hero copy matches the approved source of truth exactly` — asserts `headline`, `sub`, `ctaPrimary`, `ctaSecondary` equal the approved strings.
2. `v3 hero does not contain the forbidden 'business AI' variant` — asserts `"The execution layer for business AI."` is absent.
3. `hero renders approved headline, sub, and CTA labels with correct destinations` — Playwright navigates `/`, asserts `#hero-heading` text, sub text, primary CTA label + `href="#contact"`, secondary CTA label + `href="#maax"`, and absence of the forbidden phrase.

Pre-existing suites left intact: shape validation for all copy variants, consent/legal anchors, `hero.spec.ts`, `hero-video.spec.ts`, `hero-video-lazy.spec.ts`.

## 8. Commands Executed

- `bun run tsgo` (typecheck on modified files' impact)
- File-modification tools on `src/copy/v3.ts` and `tests/accessibility/copy-validation.spec.ts`

No `bun add`, no `bun run build`, no `playwright install`, no git state changes, no publish/deploy.

## 9. Test Results

- Typecheck (`bun run tsgo`): PASS.
- Playwright regression suites (`bunx playwright test tests/accessibility/copy-validation.spec.ts tests/accessibility/hero.spec.ts`): NOT EXECUTED in this environment. The new copy-lock unit-style assertions are deterministic against `src/copy/v3.ts`; the rendered-hero Playwright assertion depends on the dev server running Playwright's browser binaries and was not run here.

## 10. Visual Verification Status

MANUAL only. No visual-regression baseline was created for the hero (per Phase 1 scope: "no new VR framework"). Existing `tests/accessibility/visual-regression.spec.ts` covers solution/privacy/terms pages, not the hero. Founder-side visual confirmation in the live preview is required.

## 11. Remaining Risks

- `src/routes/index.tsx` JSON-LD `slogan` and `tests/accessibility/__snapshots__/jsonld.snapshot.json` still contain `operational AI` phrasing — out of Phase 1 scope.
- Playwright rendered-hero assertion not executed in this environment; unit-level copy assertions cover the source of truth but not the DOM.
- No automated hero visual-regression baseline exists.

## 12. Confirmation of No Out-of-Scope Changes

- Hero source (`src/components/cyryx/Hero.tsx`) modified: NO.
- Any image, video, poster, animation, CSS, route, navigation, or homepage section modified: NO.
- Anything committed, pushed, published, or deployed: NO.
- Approved headline, supporting copy, and CTA labels match exactly: YES.
- Phrase `The execution layer for business AI.` present anywhere in the public hero: NO.
- Visual verification: MANUAL (not automated).

PHASE 1 EVIDENCE REPORT COMPLETE — NO ADDITIONAL FILES MODIFIED.
